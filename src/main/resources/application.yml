spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/EnergyComparator
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
